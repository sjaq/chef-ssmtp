# "ssmtp" configuration file automatically written by chef.  Changes will be overwritten.
<%= "Root=#{node['ssmtp']['root_user'] }" unless node['ssmtp']['root_user'].empty? %>
<%= "MailHub=#{node['ssmtp']['mail_hub']}" unless node['ssmtp']['mail_hub'].empty? %>
<%= "rewriteDomain=#{node['ssmtp']['rewrite_domain']}" unless node['ssmtp']['rewrite_domain'].empty? %>
<%= "hostname=#{node['ssmtp']['hostname']}" unless node['ssmtp']['hostname'].empty? %>
<%= "FromLineOverride=#{node['ssmtp']['from_line_override']}" unless node['ssmtp']['from_line_override'].empty? %>
<%= "UseTLS=#{node['ssmtp']['use_tls']}" unless node['ssmtp']['use_tls'].empty? %>
<%= "UseSTARTTLS=#{node['ssmtp']['use_starttls']}" unless node['ssmtp']['use_starttls'].empty? %>
<%= "TLSCert=#{node['ssmtp']['tls_cert']}" unless node['ssmtp']['tls_cert'].empty? %>
<%= "AuthUser=#{node['ssmtp']['auth_user']}" unless node['ssmtp']['auth_user'].empty? %>
<%= "AuthPass=#{node['ssmtp']['auth_pass']}" unless node['ssmtp']['auth_pass'].empty? %>
<%= "AuthMethod=#{node['ssmtp']['auth_method']}" unless node['ssmtp']['auth_method'].empty? %>